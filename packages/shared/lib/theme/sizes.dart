library shared;

import 'package:flutter/material.dart';

const double defaultSearchMinHeight = 40;

const double defaultSearchMaxWidth = 344;

const BoxConstraints defaultSearchBoxConstraints = BoxConstraints(
    minHeight: defaultSearchMinHeight,
    maxWidth: defaultSearchMaxWidth
);

const double defaultAppBarToolbarHeight = 142;

const double defaultColumnItemHeight = 16;

const double defaultIconSize = 32;

const double defaultCardIconSize = 32;

const double defaultCardContentDividerHeight = 1;
