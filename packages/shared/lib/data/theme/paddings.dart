library shared;

import 'package:flutter/material.dart';

const EdgeInsetsGeometry defaultCardContentIconTextGroupPadding =
    EdgeInsets.only(left: 10);

const EdgeInsetsGeometry defaultCardContentGroupPadding =
    EdgeInsets.only(top: 5);

const EdgeInsetsGeometry defaultIconSpaceBetween = EdgeInsets.only(left: 10);

const EdgeInsetsGeometry defaultInfoListPadding = EdgeInsets.all(10);

const EdgeInsetsGeometry defaultCardContentDescriptionPadding =
    EdgeInsets.only(top: 15);

const EdgeInsetsGeometry defaultCardContentPadding = EdgeInsets.all(10);

const EdgeInsetsGeometry defaultCardItemPadding = EdgeInsets.all(10);

const EdgeInsetsGeometry defaultSearchPadding = EdgeInsets.all(5);

const EdgeInsetsGeometry defaultAlertPadding =
    EdgeInsets.symmetric(vertical: 11, horizontal: 0.5);

const EdgeInsetsGeometry defaultSignInBodyPadding = EdgeInsets.only(top: 43);

const Size defaultSignInAppBarSize = Size.fromHeight(88);

const EdgeInsetsGeometry defaultSignInContainerPadding =
    EdgeInsets.symmetric(vertical: 35, horizontal: 16);
